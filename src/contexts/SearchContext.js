// import { createContext, useState } from 'react';

// const SearchContext = createContext();

// //is a prop. children represents all components that are wrapped around context provider
// export function SearchProvider({ children }) {
//   return <SearchContext.Provider value={{ item: 1 }}>{children}</SearchContext.Provider>;
// }
// //context provider provides data to its children
// export default SearchContext;
